/**
 * Generated by orval v7.12.2 🍺
 * Do not edit manually.
 * hello world
 * OpenAPI spec version: 1.0.0
 */
import { z as zod } from "zod";

export const postBrandsBodyNameMin = 2;
export const postBrandsBodyNameMax = 100;
export const postBrandsBodyEnNameMaxOne = 200;
export const postBrandsBodyLogoUrlMaxOne = 1000;
export const postBrandsBodyWebsiteMaxOne = 1000;
export const postBrandsBodyDescriptionMaxOne = 200;

export const postBrandsBody = zod.object({
  name: zod.coerce
    .string({
      message: "نام برند الزامی است",
    })
    .min(postBrandsBodyNameMin, {
      message: `نام برند باید حداقل ${postBrandsBodyNameMin} کاراکتر باشد`,
    })
    .max(postBrandsBodyNameMax, {
      message: `نام برند نمی‌تواند بیشتر از ${postBrandsBodyNameMax} کاراکتر باشد`,
    })
    .describe("نام برند (الزامی)"),

  enName: zod
    .union([
      zod.coerce.string().max(postBrandsBodyEnNameMaxOne, {
        message: `نام انگلیسی برند نمی‌تواند بیشتر از ${postBrandsBodyEnNameMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("نام انگلیسی برند"),

  logoUrl: zod
    .union([
      zod
        .url({
          message: "آدرس لوگو باید یک URL معتبر باشد",
        })
        .max(postBrandsBodyLogoUrlMaxOne, {
          message: `طول آدرس لوگو نمی‌تواند بیشتر از ${postBrandsBodyLogoUrlMaxOne} کاراکتر باشد`,
        }),
      zod.null(),
    ])
    .optional()
    .describe("آدرس تصویر لوگو"),

  website: zod
    .union([
      zod
        .url({
          message: "آدرس وب‌سایت باید یک URL معتبر باشد",
        })
        .max(postBrandsBodyWebsiteMaxOne, {
          message: `طول آدرس وب‌سایت نمی‌تواند بیشتر از ${postBrandsBodyWebsiteMaxOne} کاراکتر باشد`,
        }),
      zod.null(),
    ])
    .optional()
    .describe("وب‌سایت رسمی برند"),

  description: zod
    .union([
      zod.coerce.string().max(postBrandsBodyDescriptionMaxOne, {
        message: `توضیحات نمی‌تواند بیشتر از ${postBrandsBodyDescriptionMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("توضیحات برند"),

  sortOrder: zod.coerce
    .number({
      message: "ترتیب نمایش باید عدد باشد",
    })
    .describe("اولویت نمایش برند"),

  isActive: zod.coerce
    .boolean({
      message: "وضعیت فعال/غیرفعال باید boolean باشد",
    })
    .describe("وضعیت فعال بودن برند"),
});

export const getBrandsQueryFieldDefaultOne = "createdAt";
export const getBrandsQueryFieldMaxOne = 200;
export const getBrandsQueryOrderDefaultOne = "desc";
export const getBrandsQuerySkipMin = 0;
export const getBrandsQueryLimitMax = 20;
export const getBrandsQueryNameMaxOne = 200;
export const getBrandsQueryEnNameMaxOne = 200;
export const getBrandsQueryWebsiteMaxOne = 200;

export const getBrandsQueryParams = zod.object({
  field: zod
    .union([
      zod.coerce.string().max(getBrandsQueryFieldMaxOne, {
        message: `طول نام فیلد نمی‌تواند بیشتر از ${getBrandsQueryFieldMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("فیلد مرتب‌سازی"),

  order: zod
    .union([
      zod.enum(["asc", "desc"], {
        message: "مقدار order فقط می‌تواند یکی از مقادیر asc یا desc باشد",
      }),
      zod.null(),
    ])
    .optional()
    .describe("ترتیب مرتب‌سازی (صعودی یا نزولی)"),

  skip: zod.coerce
    .number({
      message: "مقدار skip باید عدد باشد",
    })
    .min(getBrandsQuerySkipMin, {
      message: `skip نمی‌تواند کمتر از ${getBrandsQuerySkipMin} باشد`,
    })
    .describe("تعداد رکوردهای قابل رد شدن"),

  limit: zod.coerce
    .number({
      message: "مقدار limit باید عدد باشد",
    })
    .min(1, { message: "حداقل مقدار limit باید 1 باشد" })
    .max(getBrandsQueryLimitMax, {
      message: `limit نمی‌تواند بیشتر از ${getBrandsQueryLimitMax} باشد`,
    })
    .describe("حداکثر تعداد رکوردها"),

  deletedAt: zod
    .union([
      zod.iso.datetime({
        message: "فرمت تاریخ معتبر نیست",
      }),
      zod.null(),
    ])
    .optional()
    .describe("تاریخ حذف (معمولاً null)"),

  name: zod
    .union([
      zod.coerce.string().max(getBrandsQueryNameMaxOne, {
        message: `نام نمی‌تواند بیشتر از ${getBrandsQueryNameMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر براساس نام برند"),

  enName: zod
    .union([
      zod.coerce.string().max(getBrandsQueryEnNameMaxOne, {
        message: `نام انگلیسی نمی‌تواند بیشتر از ${getBrandsQueryEnNameMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر براساس نام انگلیسی"),

  website: zod
    .union([
      zod.coerce.string().max(getBrandsQueryWebsiteMaxOne, {
        message: `طول وب‌سایت نمی‌تواند بیشتر از ${getBrandsQueryWebsiteMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر براساس وب‌سایت"),

  sortOrder: zod.coerce
    .number({
      message: "sortOrder باید عدد باشد",
    })
    .optional()
    .describe("فیلتر براساس ترتیب نمایش"),

  isActive: zod
    .union([
      zod.coerce.boolean({
        message: "isActive باید boolean باشد",
      }),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر براساس وضعیت فعال بودن"),
});

export const getBrandsResponse = zod
  .object({
    success: zod
      .union([zod.coerce.boolean(), zod.null()])
      .optional()
      .describe("وضعیت موفقیت عملیات"),
  })
  .describe("ساختار استاندارد پاسخ API");

export const getBrandsIdParams = zod.object({
  id: zod.uuid({ message: "شناسه برند معتبر نیست" }).describe("شناسه برند"),
});

export const getBrandsIdResponse = zod
  .object({
    success: zod
      .union([zod.coerce.boolean(), zod.null()])
      .optional()
      .describe("وضعیت موفقیت عملیات"),
  })
  .describe("ساختار استاندارد پاسخ API");

export const patchBrandsIdParams = zod.object({
  id: zod.uuid({ message: "شناسه برند معتبر نیست" }).describe("شناسه برند"),
});

export const patchBrandsIdBodyNameMin = 2;
export const patchBrandsIdBodyNameMax = 100;
export const patchBrandsIdBodyEnNameMaxOne = 200;
export const patchBrandsIdBodyLogoUrlMaxOne = 1000;
export const patchBrandsIdBodyWebsiteMaxOne = 1000;
export const patchBrandsIdBodyDescriptionMaxOne = 200;

export const patchBrandsIdBody = zod.object({
  name: zod.coerce
    .string()
    .min(patchBrandsIdBodyNameMin, {
      message: `نام برند باید حداقل ${patchBrandsIdBodyNameMin} کاراکتر باشد`,
    })
    .max(patchBrandsIdBodyNameMax, {
      message: `نام برند نمی‌تواند بیشتر از ${patchBrandsIdBodyNameMax} کاراکتر باشد`,
    })
    .optional()
    .describe("نام برند (اختیاری)"),

  enName: zod
    .union([
      zod.coerce.string().max(patchBrandsIdBodyEnNameMaxOne, {
        message: `نام انگلیسی نمی‌تواند بیشتر از ${patchBrandsIdBodyEnNameMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("نام انگلیسی (اختیاری)"),

  logoUrl: zod
    .union([
      zod
        .url({
          message: "آدرس لوگو باید یک URL معتبر باشد",
        })
        .max(patchBrandsIdBodyLogoUrlMaxOne, {
          message: `آدرس لوگو نمی‌تواند بیشتر از ${patchBrandsIdBodyLogoUrlMaxOne} کاراکتر باشد`,
        }),
      zod.null(),
    ])
    .optional()
    .describe("آدرس لوگو (اختیاری)"),

  website: zod
    .union([
      zod
        .url({
          message: "آدرس وب‌سایت باید یک URL معتبر باشد",
        })
        .max(patchBrandsIdBodyWebsiteMaxOne, {
          message: `آدرس وب‌سایت نمی‌تواند بیشتر از ${patchBrandsIdBodyWebsiteMaxOne} کاراکتر باشد`,
        }),
      zod.null(),
    ])
    .optional()
    .describe("وب‌سایت (اختیاری)"),

  description: zod
    .union([
      zod.coerce.string().max(patchBrandsIdBodyDescriptionMaxOne, {
        message: `توضیحات نمی‌تواند بیشتر از ${patchBrandsIdBodyDescriptionMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("توضیحات (اختیاری)"),

  sortOrder: zod.coerce
    .number({
      message: "sortOrder باید عدد باشد",
    })
    .optional()
    .describe("اولویت نمایش (اختیاری)"),

  isActive: zod.coerce
    .boolean({
      message: "isActive باید boolean باشد",
    })
    .optional()
    .describe("وضعیت فعال بودن (اختیاری)"),
});

export const patchBrandsIdResponse = zod
  .object({
    success: zod
      .union([zod.coerce.boolean(), zod.null()])
      .optional()
      .describe("وضعیت موفقیت عملیات"),
  })
  .describe("ساختار استاندارد پاسخ API");

export const deleteBrandsIdParams = zod.object({
  id: zod.uuid({ message: "شناسه برند معتبر نیست" }).describe("شناسه برند"),
});
