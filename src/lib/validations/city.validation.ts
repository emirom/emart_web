/**
 * Generated by orval v7.12.2 🍺
 * Do not edit manually.
 * hello world
 * OpenAPI spec version: 1.0.0
 */
import { z as zod } from "zod";

/**
 * Create a new city
 */
export const postCitiesBodyNameMin = 2;
export const postCitiesBodyNameMax = 100;
export const postCitiesBodyAbbMaxOne = 4;

export const postCitiesBody = zod.object({
  name: zod.coerce
    .string()
    .min(
      postCitiesBodyNameMin,
      `نام شهر باید حداقل ${postCitiesBodyNameMin} کاراکتر باشد`,
    )
    .max(
      postCitiesBodyNameMax,
      `نام شهر نمی‌تواند بیش از ${postCitiesBodyNameMax} کاراکتر باشد`,
    )
    .describe("نام شهر (الزامی)"),
  abb: zod
    .union([
      zod.coerce
        .string()
        .max(
          postCitiesBodyAbbMaxOne,
          `اختصار شهر نمی‌تواند بیش از ${postCitiesBodyAbbMaxOne} کاراکتر باشد`,
        ),
      zod.null(),
    ])
    .optional()
    .describe("اختصار شهر (اختیاری)"),
  provinceId: zod
    .uuid("شناسه استان معتبر نیست")
    .describe("شناسه استان (الزامی)"),
});

/**
 * Get all cities with pagination and filters
 */
export const getCitiesQueryFieldDefaultOne = "createdAt";
export const getCitiesQueryFieldMaxOne = 200;
export const getCitiesQueryOrderDefaultOne = "desc";
export const getCitiesQuerySkipMin = 0;
export const getCitiesQueryLimitMax = 20;
export const getCitiesQuerySearchMaxOne = 100;

export const getCitiesQueryParams = zod.object({
  field: zod
    .union([
      zod.coerce
        .string()
        .max(
          getCitiesQueryFieldMaxOne,
          `نام فیلد نمی‌تواند بیش از ${getCitiesQueryFieldMaxOne} کاراکتر باشد`,
        ),
      zod.null(),
    ])
    .optional()
    .describe("فیلد مرتب‌سازی (اختیاری)"),

  order: zod
    .union([zod.enum(["asc", "desc"]), zod.null()])
    .optional()
    .describe("ترتیب مرتب‌سازی (اختیاری)"),

  skip: zod.coerce
    .number()
    .min(
      getCitiesQuerySkipMin,
      `مقدار skip نمی‌تواند کمتر از ${getCitiesQuerySkipMin} باشد`,
    )
    .describe("تعداد رکوردهای رد شده (برای صفحه‌بندی)"),

  limit: zod.coerce
    .number()
    .min(1, "حداقل تعداد قابل دریافت ۱ است")
    .max(
      getCitiesQueryLimitMax,
      `حداکثر تعداد قابل دریافت ${getCitiesQueryLimitMax} است`,
    )
    .describe("تعداد رکوردهای قابل دریافت"),

  provinceId: zod
    .union([zod.uuid("شناسه استان معتبر نیست"), zod.null()])
    .optional()
    .describe("فیلتر بر اساس شناسه استان"),

  search: zod
    .union([
      zod.coerce
        .string()
        .max(
          getCitiesQuerySearchMaxOne,
          `عبارت جستجو نمی‌تواند بیش از ${getCitiesQuerySearchMaxOne} کاراکتر باشد`,
        ),
      zod.null(),
    ])
    .optional()
    .describe("جستجو در نام شهر"),
});

export const getCitiesResponse = zod
  .object({
    success: zod.union([zod.coerce.boolean(), zod.null()]).optional(),
  })
  .describe("ساختار پاسخ استاندارد API");

/**
 * Get a single city by ID
 */
export const getCitiesIdParams = zod.object({
  id: zod.uuid("شناسه شهر معتبر نیست").describe("شناسه شهر"),
});

export const getCitiesIdResponseDataAbbMaxOne = 4;

export const getCitiesIdResponse = zod.object({
  success: zod.coerce.boolean().describe("وضعیت عملیات"),
  data: zod.object({
    id: zod.uuid().describe("شناسه یکتا (UUIDv4)"),
    createdAt: zod.iso.datetime({}).describe("تاریخ ایجاد (ISO 8601)"),
    updatedAt: zod.iso
      .datetime({})
      .describe("تاریخ آخرین به‌روزرسانی (ISO 8601)"),
    name: zod.coerce.string().describe("نام شهر"),
    abb: zod
      .union([
        zod.coerce
          .string()
          .max(
            getCitiesIdResponseDataAbbMaxOne,
            `اختصار نمی‌تواند بیش از ${getCitiesIdResponseDataAbbMaxOne} کاراکتر باشد`,
          ),
        zod.null(),
      ])
      .optional(),
    provinceId: zod.uuid("شناسه استان معتبر نیست").describe("شناسه استان"),
  }),
});

/**
 * Update a city
 */
export const patchCitiesIdParams = zod.object({
  id: zod.uuid("شناسه شهر معتبر نیست").describe("شناسه شهر"),
});

export const patchCitiesIdBodyNameMin = 2;
export const patchCitiesIdBodyNameMax = 100;
export const patchCitiesIdBodyAbbMaxOne = 4;

export const patchCitiesIdBody = zod.object({
  name: zod.coerce
    .string()
    .min(
      patchCitiesIdBodyNameMin,
      `نام شهر باید حداقل ${patchCitiesIdBodyNameMin} کاراکتر باشد`,
    )
    .max(
      patchCitiesIdBodyNameMax,
      `نام شهر نمی‌تواند بیش از ${patchCitiesIdBodyNameMax} کاراکتر باشد`,
    )
    .optional()
    .describe("نام شهر (اختیاری)"),

  abb: zod
    .union([
      zod.coerce
        .string()
        .max(
          patchCitiesIdBodyAbbMaxOne,
          `اختصار نمی‌تواند بیش از ${patchCitiesIdBodyAbbMaxOne} کاراکتر باشد`,
        ),
      zod.null(),
    ])
    .optional()
    .describe("اختصار شهر (اختیاری)"),

  provinceId: zod
    .uuid("شناسه استان معتبر نیست")
    .optional()
    .describe("شناسه استان (اختیاری)"),
});

export const patchCitiesIdResponseDataAbbMaxOne = 4;

export const patchCitiesIdResponse = zod.object({
  success: zod.coerce.boolean().describe("وضعیت عملیات"),
  data: zod.object({
    id: zod.uuid().describe("شناسه یکتا"),
    createdAt: zod.iso.datetime({}).describe("تاریخ ایجاد"),
    updatedAt: zod.iso.datetime({}).describe("تاریخ آخرین ویرایش"),
    name: zod.coerce.string().describe("نام شهر"),
    abb: zod
      .union([
        zod.coerce
          .string()
          .max(
            patchCitiesIdResponseDataAbbMaxOne,
            `اختصار نمی‌تواند بیش از ${patchCitiesIdResponseDataAbbMaxOne} کاراکتر باشد`,
          ),
        zod.null(),
      ])
      .optional(),
    provinceId: zod.uuid("شناسه استان معتبر نیست").describe("شناسه استان"),
  }),
});

/**
 * Delete a city
 */
export const deleteCitiesIdParams = zod.object({
  id: zod.uuid("شناسه شهر معتبر نیست").describe("شناسه شهر"),
});
