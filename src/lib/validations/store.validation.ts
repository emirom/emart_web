/**
 * Generated by orval v7.12.2 🍺
 * Do not edit manually.
 * hello world
 * OpenAPI spec version: 1.0.0
 */
import { z as zod } from "zod";

/**
 * Create a new store
 */
export const postStoresBodyNameMin = 2;
export const postStoresBodyNameMax = 100;
export const postStoresBodySlugMin = 2;
export const postStoresBodySlugMax = 50;
export const postStoresBodySlugRegExp = new RegExp("^[a-z0-9-]+$");
export const postStoresBodyLogoMaxOne = 200;
export const postStoresBodyDescriptionMaxOne = 200;
export const postStoresBodyEmailMaxOne = 50;
export const postStoresBodyPhoneMaxOne = 200;
export const postStoresBodyWebsiteMaxOne = 1000;

export const postStoresBody = zod
  .object({
    name: zod.coerce
      .string()
      .min(
        postStoresBodyNameMin,
        "نام فروشگاه الزامی است و باید حداقل ۲ کاراکتر باشد.",
      )
      .max(
        postStoresBodyNameMax,
        "نام فروشگاه نمی‌تواند بیش از ۱۰۰ کاراکتر باشد.",
      )
      .describe("نام فروشگاه (منحصربه‌فرد)"),

    slug: zod.coerce
      .string()
      .min(
        postStoresBodySlugMin,
        "اسلاگ الزامی است و باید حداقل ۲ کاراکتر باشد.",
      )
      .max(postStoresBodySlugMax, "اسلاگ نمی‌تواند بیش از ۵۰ کاراکتر باشد.")
      .regex(
        postStoresBodySlugRegExp,
        "اسلاگ فقط باید شامل حروف کوچک، عدد و خط تیره باشد.",
      )
      .describe("اسلاگ آدرس (منحصربه‌فرد، فقط حروف کوچک و خط تیره)"),

    logo: zod
      .union([
        zod.coerce
          .string()
          .max(
            postStoresBodyLogoMaxOne,
            "آدرس لوگو نمی‌تواند بیش از ۲۰۰ کاراکتر باشد.",
          ),
        zod.null(),
      ])
      .optional()
      .describe("آدرس لوگوی فروشگاه"),

    description: zod
      .union([
        zod.coerce
          .string()
          .max(
            postStoresBodyDescriptionMaxOne,
            "توضیحات نمی‌تواند بیش از ۲۰۰ کاراکتر باشد.",
          ),
        zod.null(),
      ])
      .optional()
      .describe("توضیحات فروشگاه"),

    email: zod
      .union([
        zod
          .email("ایمیل معتبر نیست.")
          .max(
            postStoresBodyEmailMaxOne,
            "ایمیل نمی‌تواند بیش از ۵۰ کاراکتر باشد.",
          ),
        zod.null(),
      ])
      .optional()
      .describe("ایمیل کسب‌وکار (منحصربه‌فرد)"),

    phone: zod
      .union([
        zod.coerce
          .string()
          .max(
            postStoresBodyPhoneMaxOne,
            "شماره تماس نمی‌تواند بیش از ۲۰۰ کاراکتر باشد.",
          ),
        zod.null(),
      ])
      .optional()
      .describe("شماره تماس کسب‌وکار (منحصربه‌فرد)"),

    website: zod
      .union([
        zod
          .url("آدرس وب‌سایت معتبر نیست.")
          .max(
            postStoresBodyWebsiteMaxOne,
            "آدرس وب‌سایت نمی‌تواند بیش از ۱۰۰۰ کاراکتر باشد.",
          ),
        zod.null(),
      ])
      .optional()
      .describe("آدرس وب‌سایت بیرونی"),

    isActive: zod.coerce.boolean().describe("فعال بودن فروشگاه"),

    isApproved: zod.coerce.boolean().describe("وضعیت تأیید توسط پلتفرم"),

    ownerId: zod
      .union([zod.uuid("شناسه مالک معتبر نیست."), zod.null()])
      .optional()
      .describe("شناسه کاربر مالک"),
  })
  .describe("بدنه‌ی ایجاد فروشگاه");

/**
 * List all stores with pagination and filters
 */
export const getStoresQueryFieldDefaultOne = "createdAt";
export const getStoresQueryFieldMaxOne = 200;
export const getStoresQueryOrderDefaultOne = "desc";
export const getStoresQuerySkipMin = 0;
export const getStoresQueryLimitMax = 20;
export const getStoresQueryNameMaxOne = 200;
export const getStoresQuerySlugMaxOne = 200;
export const getStoresQueryEmailMaxOne = 200;
export const getStoresQueryPhoneMaxOne = 200;

export const getStoresQueryParams = zod.object({
  field: zod
    .union([
      zod.coerce
        .string()
        .max(getStoresQueryFieldMaxOne, "طول فیلد بیش از حد مجاز است."),
      zod.null(),
    ])
    .optional()
    .describe("مرتب‌سازی بر اساس فیلد"),

  order: zod
    .union([zod.enum(["asc", "desc"], "مقدار ترتیب نامعتبر است."), zod.null()])
    .optional()
    .describe("نوع ترتیب"),

  skip: zod.coerce
    .number()
    .min(getStoresQuerySkipMin, "مقدار skip نمی‌تواند منفی باشد.")
    .describe("تعداد رد کردن رکوردها"),

  limit: zod.coerce
    .number()
    .min(1, "حداقل مقدار limit برابر با ۱ است.")
    .max(getStoresQueryLimitMax, "حداکثر مقدار limit برابر با ۲۰ است.")
    .describe("حداکثر تعداد رکوردهای بازگشتی"),

  name: zod
    .union([
      zod.coerce
        .string()
        .max(getStoresQueryNameMaxOne, "نام فروشگاه طولانی است."),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر براساس نام فروشگاه"),

  slug: zod
    .union([
      zod.coerce.string().max(getStoresQuerySlugMaxOne, "اسلاگ طولانی است."),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر براساس اسلاگ"),

  ownerId: zod
    .union([zod.uuid("شناسه مالک معتبر نیست."), zod.null()])
    .optional()
    .describe("فیلتر براساس شناسه مالک"),

  isActive: zod
    .union([zod.coerce.boolean(), zod.null()])
    .optional()
    .describe("فیلتر براساس وضعیت فعال بودن"),

  isApproved: zod
    .union([zod.coerce.boolean(), zod.null()])
    .optional()
    .describe("فیلتر براساس وضعیت تأیید"),

  email: zod
    .union([
      zod.coerce.string().max(getStoresQueryEmailMaxOne, "ایمیل طولانی است."),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر براساس ایمیل"),

  phone: zod
    .union([
      zod.coerce
        .string()
        .max(getStoresQueryPhoneMaxOne, "شماره تماس طولانی است."),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر براساس شماره تماس"),
});

export const getStoresResponse = zod
  .object({
    success: zod
      .union([zod.coerce.boolean(), zod.null()])
      .optional()
      .describe("وضعیت عملیات"),
  })
  .describe("فرمت استاندارد پاسخ‌گویی API")
  .describe("پاسخ لیست فروشگاه‌ها");

/**
 * Get a single store by ID
 */
export const getStoresIdParams = zod.object({
  id: zod.uuid("شناسه فروشگاه معتبر نیست.").describe("شناسه فروشگاه"),
});

export const getStoresIdResponse = zod
  .object({
    success: zod
      .union([zod.coerce.boolean(), zod.null()])
      .optional()
      .describe("وضعیت عملیات"),
  })
  .describe("فرمت استاندارد پاسخ")
  .describe("پاسخ دریافت یک فروشگاه");

/**
 * Update a store
 */
export const patchStoresIdParams = zod.object({
  id: zod.uuid("شناسه فروشگاه معتبر نیست.").describe("شناسه فروشگاه"),
});

export const patchStoresIdBodyNameMin = 2;
export const patchStoresIdBodyNameMax = 100;
export const patchStoresIdBodyLogoMaxOne = 200;
export const patchStoresIdBodyDescriptionMaxOne = 200;
export const patchStoresIdBodyEmailMaxOne = 50;
export const patchStoresIdBodyPhoneMaxOne = 200;
export const patchStoresIdBodyWebsiteMaxOne = 1000;

export const patchStoresIdBody = zod
  .object({
    name: zod.coerce
      .string()
      .min(patchStoresIdBodyNameMin, "نام فروشگاه باید حداقل ۲ کاراکتر باشد.")
      .max(
        patchStoresIdBodyNameMax,
        "نام فروشگاه نمی‌تواند بیش از ۱۰۰ کاراکتر باشد.",
      )
      .optional()
      .describe("نام فروشگاه (منحصربه‌فرد)"),

    logo: zod
      .union([
        zod.coerce
          .string()
          .max(patchStoresIdBodyLogoMaxOne, "آدرس لوگو بیش از حد طولانی است."),
        zod.null(),
      ])
      .optional()
      .describe("آدرس لوگوی فروشگاه"),

    description: zod
      .union([
        zod.coerce
          .string()
          .max(
            patchStoresIdBodyDescriptionMaxOne,
            "توضیحات بیش از حد طولانی است.",
          ),
        zod.null(),
      ])
      .optional()
      .describe("توضیحات فروشگاه"),

    email: zod
      .union([
        zod
          .email("ایمیل معتبر نیست.")
          .max(
            patchStoresIdBodyEmailMaxOne,
            "ایمیل نمی‌تواند بیش از ۵۰ کاراکتر باشد.",
          ),
        zod.null(),
      ])
      .optional()
      .describe("ایمیل کسب‌وکار (منحصربه‌فرد)"),

    phone: zod
      .union([
        zod.coerce
          .string()
          .max(
            patchStoresIdBodyPhoneMaxOne,
            "شماره تماس بیش از حد طولانی است.",
          ),
        zod.null(),
      ])
      .optional()
      .describe("شماره تماس کسب‌وکار (منحصربه‌فرد)"),

    website: zod
      .union([
        zod
          .url("آدرس وب‌سایت معتبر نیست.")
          .max(patchStoresIdBodyWebsiteMaxOne, "آدرس وب‌سایت طولانی است."),
        zod.null(),
      ])
      .optional()
      .describe("آدرس وب‌سایت بیرونی"),

    isActive: zod.coerce.boolean().optional().describe("فعال بودن فروشگاه"),

    isApproved: zod.coerce
      .boolean()
      .optional()
      .describe("وضعیت تأیید توسط پلتفرم"),

    ownerId: zod
      .union([zod.uuid("شناسه مالک معتبر نیست."), zod.null()])
      .optional()
      .describe("شناسه کاربر مالک"),
  })
  .describe("بدنه‌ی بروزرسانی فروشگاه");

export const patchStoresIdResponse = zod
  .object({
    success: zod
      .union([zod.coerce.boolean(), zod.null()])
      .optional()
      .describe("وضعیت عملیات"),
  })
  .describe("فرمت استاندارد پاسخ")
  .describe("پاسخ بروزرسانی فروشگاه");

/**
 * Deactivate a store (soft delete)
 */
export const deleteStoresIdParams = zod.object({
  id: zod.uuid("شناسه فروشگاه معتبر نیست.").describe("شناسه فروشگاه"),
});
