/**
 * Generated by orval v7.12.2 🍺
 * Do not edit manually.
 * hello world
 * OpenAPI spec version: 1.0.0
 */
import { z as zod } from "zod";

export const postUnitsBodyTitleMin = 2;
export const postUnitsBodyTitleMax = 100;

export const postUnitsBody = zod
  .object({
    title: zod.coerce
      .string()
      .min(postUnitsBodyTitleMin, {
        message: `عنوان واحد باید حداقل ${postUnitsBodyTitleMin} کاراکتر باشد`,
      })
      .max(postUnitsBodyTitleMax, {
        message: `عنوان واحد نمی‌تواند بیشتر از ${postUnitsBodyTitleMax} کاراکتر باشد`,
      })
      .describe("عنوان یا نام واحد اندازه‌گیری"),
  })
  .describe("بدنه درخواست برای ایجاد واحد اندازه‌گیری جدید");

/**
 * Get all units with pagination and filtering
 */
export const getUnitsQueryFieldDefaultOne = "createdAt";
export const getUnitsQueryFieldMaxOne = 200;
export const getUnitsQueryOrderDefaultOne = "desc";
export const getUnitsQuerySkipMin = 0;
export const getUnitsQueryLimitMax = 20;
export const getUnitsQueryTitleMaxOne = 200;

export const getUnitsQueryParams = zod.object({
  field: zod
    .union([
      zod.coerce.string().max(getUnitsQueryFieldMaxOne, {
        message: `طول فیلد مرتب‌سازی نمی‌تواند بیشتر از ${getUnitsQueryFieldMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("مرتب‌سازی براساس فیلد مشخص"),

  order: zod
    .union([
      zod.enum(["asc", "desc"], {
        message: "ترتیب مرتب‌سازی باید asc یا desc باشد",
      }),
      zod.null(),
    ])
    .optional()
    .describe("ترتیب مرتب‌سازی"),

  skip: zod.coerce
    .number()
    .min(getUnitsQuerySkipMin, {
      message: `حداقل مقدار قابل قبول برای skip برابر ${getUnitsQuerySkipMin} است`,
    })
    .describe("تعداد رکوردهایی که باید رد شوند"),

  limit: zod.coerce
    .number()
    .min(1, { message: "حداقل مقدار limit باید 1 باشد" })
    .max(getUnitsQueryLimitMax, {
      message: `حداکثر مقدار limit برابر ${getUnitsQueryLimitMax} است`,
    })
    .describe("تعداد رکوردهای قابل دریافت"),

  title: zod
    .union([
      zod.coerce.string().max(getUnitsQueryTitleMaxOne, {
        message: `طول عنوان جستجو نمی‌تواند بیشتر از ${getUnitsQueryTitleMaxOne} کاراکتر باشد`,
      }),
      zod.null(),
    ])
    .optional()
    .describe("فیلتر واحدها براساس عنوان"),
});

export const getUnitsResponse = zod
  .object({
    success: zod
      .union([zod.coerce.boolean(), zod.null()])
      .optional()
      .describe("وضعیت انجام عملیات"),
  })
  .describe("ساختار استاندارد پاسخ API");

export const getUnitsIdParams = zod.object({
  id: zod
    .uuid({
      message: "شناسه واحد معتبر نیست",
    })
    .describe("شناسه یکتای واحد"),
});

export const getUnitsIdResponse = zod
  .object({
    success: zod
      .union([zod.coerce.boolean(), zod.null()])
      .optional()
      .describe("وضعیت انجام عملیات"),
  })
  .describe("ساختار پاسخ API برای دریافت یک واحد");

/**
 * Update unit
 */
export const patchUnitsIdParams = zod.object({
  id: zod
    .uuid({
      message: "شناسه واحد معتبر نیست",
    })
    .describe("شناسه یکتای واحد"),
});

export const patchUnitsIdBodyTitleMin = 2;
export const patchUnitsIdBodyTitleMax = 100;

export const patchUnitsIdBody = zod
  .object({
    title: zod.coerce
      .string()
      .min(patchUnitsIdBodyTitleMin, {
        message: `عنوان واحد باید حداقل ${patchUnitsIdBodyTitleMin} کاراکتر باشد`,
      })
      .max(patchUnitsIdBodyTitleMax, {
        message: `عنوان واحد نمی‌تواند بیشتر از ${patchUnitsIdBodyTitleMax} کاراکتر باشد`,
      })
      .optional()
      .describe("عنوان یا نام واحد اندازه‌گیری"),
  })
  .describe("بدنه درخواست برای بروزرسانی واحد");

export const patchUnitsIdResponseDataTitleMin = 2;
export const patchUnitsIdResponseDataTitleMax = 100;

export const patchUnitsIdResponse = zod
  .object({
    success: zod.coerce.boolean().describe("وضعیت انجام عملیات"),
    data: zod
      .object({
        id: zod
          .uuid({
            message: "شناسه معتبر نیست",
          })
          .describe("شناسه یکتا"),

        createdAt: zod.iso.datetime({}).describe("تاریخ ایجاد (فرمت ISO 8601)"),

        updatedAt: zod.iso
          .datetime({})
          .describe("تاریخ آخرین بروزرسانی (فرمت ISO 8601)"),

        deletedAt: zod
          .union([zod.iso.datetime({}), zod.null()])
          .optional()
          .describe("تاریخ حذف (در صورت حذف نرم)"),

        title: zod.coerce
          .string()
          .min(patchUnitsIdResponseDataTitleMin, {
            message: `عنوان واحد باید حداقل ${patchUnitsIdResponseDataTitleMin} کاراکتر باشد`,
          })
          .max(patchUnitsIdResponseDataTitleMax, {
            message: `عنوان واحد نمی‌تواند بیشتر از ${patchUnitsIdResponseDataTitleMax} کاراکتر باشد`,
          })
          .describe("عنوان یا نام واحد اندازه‌گیری"),
      })
      .describe("اطلاعات کامل یک واحد"),
  })
  .describe("ساختار پاسخ بروزرسانی واحد");

export const deleteUnitsIdParams = zod.object({
  id: zod
    .uuid({
      message: "شناسه واحد معتبر نیست",
    })
    .describe("شناسه یکتای واحد"),
});
